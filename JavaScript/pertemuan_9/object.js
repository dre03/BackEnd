/**
 * membuat variabel user dengan user
 * membuat object menggunakan {}
 */
const user = {
    name : "Andre Apriyana",
    address : "Bogor",
    age : 19,
    isMarried : false,
};
// mengakses property
// mengakses property menggunakan dot atau bracket
console.log(user.name);
console.log(user.address);
console.log(user["age"]);